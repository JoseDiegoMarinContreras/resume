---
let defaultLang = 'en';
if (typeof window !== 'undefined' && typeof window.navigator !== 'undefined') {
    defaultLang = navigator.language.substring(0, 2);
}

return Astro.redirect(`/resume/${defaultLang}`);
---